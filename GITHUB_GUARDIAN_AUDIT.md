# GitHub Guardian Audit — 2026-03-29

## 🔴 Critical: CI Failing

### mcp-video
**Issue:** Ruff linter can't parse `pyproject.toml`
- Line 53 has `all = [...]` which is not a valid Ruff config field
- This breaks the lint step in CI

**Fix:** Move `all` out of `[tool.ruff]` and into `[project.optional-dependencies]`:
```toml
[project.optional-dependencies]
all = ["pillow>=10.0", "rich>=13.0", "scikit-learn>=1.3", "webcolors>=1.13", "anthropic>=0.30"]
```

### DialectOS
**Issue:** `packages/security` test failure in `RateLimiter > should use sliding window`
- 64 passed, 1 failed
- Test expects promise to resolve but rejects with `SecurityError: Rate limit exceeded`
- Likely a timing/race condition in the rate limiter test

**Fix:** 
1. Review the sliding window test at `packages/security/src/__tests__/security.test.ts:509`
2. Add proper timing assertions or mock the clock
3. Also update GitHub Actions from Node.js 20 → Node.js 24 (deprecated, deadline Sep 2026)

## ⚠️ Needs Attention

### openclaw (fork)
- 3 CI runs were cancelled (Feb 5-6)
- Consider syncing with upstream or disabling CI if not maintaining

## ✅ Healthy
- cerafica — all green
- site-to-stitch — quiet, no CI
- puenteworks-site — Pages deployment working
- cerafica-site — Pages deployment working (last active Feb 12)

## 📦 Forks (low priority)
- awesome-mcp-servers — issues disabled
- Interpreted-Context-Methdology — issues disabled

---
*Generated by Kai — GitHub Guardian*
